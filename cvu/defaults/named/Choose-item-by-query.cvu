.choose-item-by-query {
    title: {{ title }} /*or "Choose a {type}" }}*/
    editMode: true
    
    editActionButton: toggleEditMode
    actionButton:
        addDataItem {
            arguments {
                type: {{type}}
            }
        }
        
    defaultRenderer:list
    
    [datasource = pod] {
       query: "{query}"
    }
    
    [renderer = list]{
        press: [
            link {
                arguments {
                    subject: {{subject}}
                    property: {{property}}
                }
            }
            closePopup
        ]
    }
}
