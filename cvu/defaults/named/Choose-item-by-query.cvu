.choose-item-by-query {
    title: {{ title }} /*or "Choose a {type}" }}*/
    editMode: true
    
    editActionButton: toggleEditMode
    actionButton:
        addItem {
            arguments {
                type: {{type}}
            }
        }
        
    defaultRenderer:list
    
    [datasource = pod] {
       query: "{query}"
    }
    
    [renderer = list]{
        press: [
            link {
                arguments {
                    subject: {{subject}}
                    property: {{property}}
                }
            }
            closePopup
        ]
    }
}
